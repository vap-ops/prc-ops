# Site map

Audited 2026-07-11 (main 89cacf01, 0.13.0; supersedes the 2026-06-13 audit).
Every route, its gate, how users arrive, and where "back" goes. **Nav changes
must update this doc in the same unit** (same contract as ui-conventions.md).
Canonical nav-surface names + nav law: ui-conventions.md §12.

Principle: the WP list at `/projects/[id]` is THE project page for
every role (WP-centric doctrine). Round-trip rule: entering a detail
surface from a hub, the back affordance returns to that same hub.

Spec 82 (history): the URL names the surface, not the viewer's role.
Unit 1 moved the project detail surfaces `/sa/projects/*` → `/projects/*`;
Unit 2 moved reports `/pm/projects/[id]/reports` → `/projects/[id]/reports`;
Unit 3 folded the two project hubs (`/sa`, `/pm/projects`) into one `/projects`
hub (role only decides the chrome) and retired `projectHubHref`; Unit 4 moved
the remaining role-named surfaces — `/pm` → `/review`, `/pm/work-packages` →
`/review/work-packages`, `/pm/payroll` → `/payroll`, `/pm/contacts` →
`/contacts` (307 redirects keep old deep links resolving). Only Unit 5 (promote
307s → permanent, drop dead rules) remains. The lone survivor under `/pm` is the
spec-19 `/pm/requests` → `/requests` legacy 308 (out of scope; Unit 5 candidate).

`roleHome` (role-home.ts, the SSOT — refreshed 2026-07-16): site_admin → `/sa`
(spec 192 U4) · PM tier (pm/super/director) → `/dashboard` (spec 183) ·
procurement + procurement_manager → `/procurement` (spec 323 U3b, the STR
hub; was `/requests`) · project_coordinator →
`/projects` · accounting → `/accounting` · legal → `/legal` · contractor →
`/portal` · client → `/client` · technician → `/technician` · others →
`/coming-soon`.

## Bottom tabs (phones) — refreshed 2026-07-11

Per-role sets: `SA_TABS`, `PM_TABS`, `PROCUREMENT_TABS`,
`PROCUREMENT_MANAGER_TABS`, `COORDINATOR_TABS`, `ACCOUNTING_TABS`, `LEGAL_TABS`
in `bottom-tab-bar.tsx` — **the code is the SSOT**; this is a dated snapshot.
The desktop hub strip (`hub-nav.tsx` `*_HUB_NAV`) carries every bottom-tab
destination per role and may add reference surfaces the phone bar omits
(PM: + ทีมงาน `/workers`) — nav law §12, ui-conventions.md.

- SA (spec 192 U4 · 313 D2): หน้าหลัก `/sa` · โครงการ `/projects` · ทีมงาน `/team`
  · จัดซื้อ `/requests` · ตั้งค่า `/settings` — ภาพรวม dropped (the daily home
  supersedes it); ทีมงาน added as a tab (nav-coherence audit 2026-07 — /team was
  strand­ed on phones, reachable only from a home tile with no back chip).
- PM tier (pm/super/director): โครงการ · จัดซื้อ · ภาพรวม `/dashboard` (claims
  `/review`, spec 183) · คำขอสมัคร `/registrations` (spec 263/264) · ตั้งค่า.
- procurement + procurement_manager (spec 323 U3b, decision A — the STR spine;
  strip mirrors it exactly): หน้าหลัก `/procurement` · ขอบเขต
  `/procurement/scope` · เวลา `/procurement/time` · ทรัพยากร
  `/procurement/resources` · ตั้งค่า. The old flat tabs are re-homed in the
  hub: จัดซื้อ/รายงาน/ผู้ขาย/ค่าแรง as STR doors, โครงการ context as the
  project lens + per-project status strip (D3 — no /projects door; the route
  stays reachable but unlit), the manager's คำขอสมัคร as the hub nudge. A leaf
  like `/requests` lights no tab (spec-19 acceptance).
- project_coordinator: โครงการ · ตั้งค่า.
- accounting: บัญชี `/accounting` · ตั้งค่า.
- legal (spec 284 U5): กฎหมาย `/legal` · ตั้งค่า.

The ตั้งค่า tab lights (match) on `/profile`, `/contacts`, `/workers`,
`/equipment`, `/catalog`, `/payroll`, `/accounting` (spec 197 moved
`/store` + `/stock-count` to the projects tab). _Procurement exception
(spec 323 U4):_ the STR spine's ตั้งค่า tab matches only `/profile` — for the
procurement tiers the reference surfaces are hub doors (their ตั้งค่า dropped
the ข้อมูลหลัก + ค่าใช้จ่าย cards), so those routes are unlit leaves like
`/requests`. The หน้าหลัก tab carries the
SA rework badge (spec 218); จัดซื้อ + ภาพรวม carry the PM-tier decision badges
(specs 183/184).

- **Exception (Field-First reskin Unit 1):** the WP detail page
  (`/projects/[id]/work-packages/[id]`) renders NO bottom tab bar — the fixed
  capture bar takes the thumb zone and the back chip handles return. This is
  the one screen where the tab bar gives way; every other screen keeps it.

## Entry, auth and account

| Route                 | Gate                                                                               | Rows / actions →                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Back →                                |
| --------------------- | ---------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- |
| `/` (root dispatcher) | public (session dispatch)                                                          | Reached: app root URL; landing after `/auth/logout`. Signed-in with a users row → redirect(roleHome(role)); signed-out → PRC Ops hero card + LINE LoginButton. (src/app/page.tsx; getUser() then roleHome() dispatch; missing users row falls through to the unauth UI.)                                                                                                                                                                                              | none (bespoke unauth card, no chrome) |
| `/login`              | public                                                                             | Reached: LoginButton on `/` and any unauth-guard `redirect('/login')` across the app. Already-signed-in → redirect(next ?? roleHome ?? /coming-soon); else LINE login button + optional ?error banner + ?handoff=approved success banner. (Honors validated same-origin ?next via safeNextPath; ERROR_MESSAGES map for oauth_failed/session_failed/unknown.)                                                                                                          | none (bespoke login card, no chip)    |
| `/coming-soon`        | session-only (getClaims, NOT requireRole)                                          | Reached: roleHome() destination for still-unbuilt roles (hr, subcon_manager, site_owner, auditor) and the visitor journey. Bounces served roles onward (site_admin→/projects, pm/director→/review, visitor→/register or VisitorLanding); super_admin renders OperatorHub (โครงการ/รอตรวจ/คำขอซื้อ/โปรไฟล์); other unserved roles get a role-labeled wait card + DisplayNameForm + logout. (spec 264 G3 / ADR 0072 visitor router; no users row → redirect('/login').) | none (bespoke landing, no chip)       |
| `/profile`            | session-only (getClaims, NOT requireRole — every authenticated role incl. visitor) | Reached: `/settings` account row (บัญชี) and the browser app-header avatar link. EmployeeIdCard (internal staff only via isEmployeeRole) + DisplayNameForm (แก้ไขชื่อที่แสดง) + LogoutButton. (spec 291 U2 — employee-ID card renders only for isEmployeeRole; client/contractor/visitor get the plain profile.)                                                                                                                                                      | chip → `/settings` (ตั้งค่า)          |
| `/stock-count`        | redirect                                                                           | Reached: legacy top-level links / muscle-memory. `redirect('/projects')` — server component, no render. (spec 197 U2 — standalone ตรวจนับ retired into the per-project คลัง page.)                                                                                                                                                                                                                                                                                    | n/a (redirect)                        |
| `/store`              | redirect                                                                           | Reached: legacy top-level links / muscle-memory. `redirect('/projects')` — server component, no render. (spec 197 U1 — store (คลัง) moved to /projects/[id]/store.)                                                                                                                                                                                                                                                                                                   | n/a (redirect)                        |
| `/contacts`           | redirect                                                                           | Reached: bare `/contacts` links + the bottom-bar `/contacts` match. `redirect('/contacts/customers')` — server component, no render. (spec 99 — Contacts split into group screens; bare path lands on the customers group.)                                                                                                                                                                                                                                           | n/a (redirect)                        |

Handlers (route handlers, not pages — no chrome, no back):

| Route                                   | Gate                                                                                                                                       | Purpose                                                                                                                                                                                                                                                                                                                                                                               |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/auth/line/start`                      | public                                                                                                                                     | GET: browser (non-PWA) LoginButton begins LINE OAuth — generate CSRF state, set httpOnly `line_oauth_state` cookie (scoped /auth/line, 600s, {s,n?}), 302 to LINE authorize URL. (ADR 0012; threads optional validated ?next into the cookie payload, never onto the LINE URL.)                                                                                                       |
| `/auth/line/callback`                   | public                                                                                                                                     | GET: LINE's post-authorize return (browser + PWA-handoff). Resolve flow (state cookie vs login_handoffs row), exchange ?code + verify id_token, provision/locate auth.users, (handoff) bind row, mint session via generateLink→verifyOtp, refresh users profile, redirect by role or validated ?next. (ADR 0012/0041; failures → /login?error=oauth_failed\|session_failed\|unknown.) |
| `/auth/handoff/start`                   | public (POST-only)                                                                                                                         | POST: installed-PWA device-code step 1 — purge expired handoffs, insert a login_handoffs row (state + device_code, 600s TTL), return {device_code, authorize_url}. (spec 43 / ADR 0041; POST-only so codes never land in URLs/prefetch.)                                                                                                                                              |
| `/auth/handoff/poll`                    | public (POST-only; device_code is the bearer)                                                                                              | POST: PWA polls after opening the authorize URL — look up by device_code; pending→{status:pending}; approved→atomic claim (status→consumed), mint session onto the PWA jar, profile write, return {status:ok, redirect:roleHome}. (Every non-claimable outcome returns {status:'expired'} — a probing client learns nothing.)                                                         |
| `/auth/logout`                          | session-only (idempotent)                                                                                                                  | POST: `supabase.auth.signOut()` + clearAssumedRoleCookie(), 303 → `/`; GET → 405. Fired by LogoutButton on /profile, /coming-soon, browser app-header. (spec 274 — also drops the super_admin view-as cookie so it can't survive sign-out.)                                                                                                                                           |
| `/api/health`                           | public                                                                                                                                     | GET: dependency-free 200 → {status:'ok', version, timestamp}; force-dynamic, no DB/auth/service-role. Uptime monitors / liveness probes. (spec 287, GA gap register G5.)                                                                                                                                                                                                              |
| `/api/telemetry`                        | session-only (getClaims; 401 on no user / missing users row)                                                                               | POST: SA usage tracker batches — validate/sanitize up to 100 events (context capped 4000 chars), insert into interaction_events via the RLS server client stamping actor_id/actor_role from the session; 202. (spec 244 U1b / ADR 0068 — identity is DB-stamped, never from the client payload.)                                                                                      |
| `/api/notifications/drain`              | secret-gated (constant-time x-drain-secret; env-gated 503 until LINE token set)                                                            | POST: pg_cron→pg_net every minute — reclaim stuck 'sending', expire >24h pending, claim a batch, enrich (WP/PM-pool/uploaders/super-pool/PO), push LINE + optional Telegram, update outbox; returns counts. (spec 32 / ADR 0037; danger-path service-role admin client.)                                                                                                              |
| `/pm/requests` (legacy redirect)        | public (proxy gates the target)                                                                                                            | GET: 308 permanent HTTP redirect → `/requests`, for legacy bookmarks / LINE links. (spec 19 §4 — a route handler, not a page, so the 308 is a real HTTP redirect, not a 200 streamed under /pm loading.tsx.)                                                                                                                                                                          |
| `/requests/reports/export`              | PURCHASE_REPORT_ROLES (procurement + procurement_manager + project_manager + project_director + super_admin + accounting — NOT site_admin) | GET (ดาวน์โหลด CSV link on /requests/reports): streams purchase_report rows as text/csv, reusing the page's exact parseReportQuery + purchase_report call so CSV and screen never disagree. (Gate pinned to equal the page gate — purchase-reports-export-gate.test.ts; no-store; purchaser column gated isProcurementManagerTier.)                                                   |
| `/payroll/export`                       | PAYROLL_VIEW_ROLES (PAYROLL_ROLES + accounting — same set as /payroll)                                                                     | GET (ดาวน์โหลด CSV `<a download>` on /payroll, ?start&end): requireRole first, then builds the ค่าแรง payroll CSV via fetchPayrollReport (admin client), returns text/csv, no-store. (spec 69; gate mirrors the /payroll page — payroll-export-gate.test.ts pins page===route.)                                                                                                       |
| `/accounting/journal/export`            | ACCOUNTING_ROLES (accounting + super_admin)                                                                                                | GET (no in-app link; direct-URL CSV, mirrors the payroll export): GL journal CSV, one row per journal LINE, UTF-8 BOM, no-store; optional ?from/&to inclusive, bad/missing → current month. (spec 288 U1; admin client behind the gate — journal_entries/lines are RLS zero-grant.)                                                                                                   |
| `/settings/wp-grouping-import/template` | requireRole(["super_admin"])                                                                                                               | GET (ดาวน์โหลดเทมเพลต button on /settings/wp-grouping-import, ?project=): returns pre-filled TSV (one row per WP, OldCode=current code) as attachment; 400 on missing ?project, 404 on unknown project. (spec 270 U2b.)                                                                                                                                                               |

## SA home surfaces

| Route                    | Gate                                                                                    | Rows / actions →                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Back →                                       |
| ------------------------ | --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- |
| `/sa`                    | requireRole([site_admin, super_admin]) — inline literal (not a role-home constant)      | SA daily home column: ต้องแก้ไข bounced/rework actions → ปัญหาวันนี้ issues → คำขอสมัครรอตรวจ card → ทีมงานวันนี้ muster → แผนวันนี้ worklist → เครื่องมือ tiles → งานของฉัน WP list, plus floating ถ่ายรูป + แจ้งปัญหา FABs. (Spec 192 U4 → 277 P0. super_admin admitted by gate but homes to /dashboard; the คำขอสมัครรอตรวจ card renders only for site_admin — super_admin uses /registrations.)                                                                                                                                                                                                                      | hub/tab (no chip)                            |
| `/team`                  | requireRole(TEAM_PAGE_ROLES — SITE_STAFF_ROLES ∪ procurement/proc_mgr, call-site union) | Reached: the ทีมงาน SA bottom tab / hub strip (spec 313 D2, nav-coherence audit 2026-07) + the SaTools ทีมงาน tile on /sa home. People hub, role-gated body: SA/super_admin (isCrew) → เช็คชื่อ CTA (musterHref of the current project) + เพิ่มช่างใหม่ AddTechnicianSheet + พิมพ์บัตรช่าง (→ /team/badges) + CrewProgressRoster (รอตรวจ→รอยืนยัน→พร้อม) + ทีมหน้างาน site-team board; STAFF_APPROVAL_ROLES → คำขอสมัคร card (→ /registrations, pending count); WORKER_ROSTER_ROLES → รายชื่อช่าง (→ /workers) + ค่าแรง (→ /payroll) tiles. (Spec 313 U1, absorbs /sa/crew. Spec 279/282/298/306.)                       | hub/tab (no chip)                            |
| `/team/badges`           | requireRole([site_admin, super_admin]) — inline literal                                 | Reached: พิมพ์บัตรช่าง (QR) button on /team. Printable QR badge sheet (name + PRC code + worker-id QR per active worker); ?worker= reprints one card. (Spec 306 U1, moved to /team by spec 313 U1.)                                                                                                                                                                                                                                                                                                                                                                                                                      | chip → `/team` (กลับ)                        |
| `/team/poster`           | requireRole([site_admin, super_admin]) — inline literal                                 | Reached: พิมพ์โปสเตอร์ button on the เพิ่มช่างใหม่ sheet's firm QR card. Printable per-firm subcon onboarding poster (?contractor=&project= — big QR of the bank-free `/register/technician?contractor=` link for the wall/morning-talk board). Forged/unknown ids resolve to the empty state via RLS. (Spec 328 U2.)                                                                                                                                                                                                                                                                                                    | chip → `/team` (กลับ)                        |
| `/procurement`           | requireRole(PROCUREMENT_HOME_ROLES — procurement/proc_mgr/super_admin, hub-body.tsx)    | Procurement's roleHome — the selection-first dashboard (spec 327). Quick door chips on top (จัดซื้อ/ของเข้า/ใบสั่งซื้อ/ทะเบียนวัสดุ — icon-only, ?from-threaded) + portfolio alert strip (เสี่ยงช้า → time?view=late · ของเข้าวันนี้ → /requests?band=in_transit; grain-labeled ทุกโครงการ) + one card per RLS-visible project (FULL list; tap = <form> → setProcurementProject cookie → /procurement/scope; FolderKanban side-door → หน้าโครงการ) + ทุกโครงการ row → /requests + ทั้งหมด labeled door grid (rule-4 path, 📍 doors via selection) + คำขอสมัคร nudge (STAFF_APPROVAL_ROLES). Reads counts/dates, never ฿. | hub/tab (no chip)                            |
| `/procurement/[section]` | same gate; unknown section → notFound() (parseProcurementSection)                       | The STR bottom-tab landing: since spec 327 U6c a section page = its door chips on top (icon-only, ?from-threaded, 📍 via the selection cookie) + its project view ONLY — **scope** WP list + supply overlay (conflict rows, no-plan doors, คลัง bucket) · **time** ?view= pills เสี่ยงช้า                                                                                                                                                                                                                                                                                                                                | สัปดาห์นี้                                   | ไทม์ไลน์ · **resources** readiness (coverage/equipment/labor slot). Grids, strip, lens and the section-page nudge retired (dashboard carries selection + alerts). | hub/tab (no chip) |
| `/sa/crew`               | redirect → `/team`                                                                      | Spec 313 U1 thin redirect (muscle memory + old links keep working; kept ≥1 release).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | (redirect)                                   |
| `/sa/crew/badges`        | redirect → `/team/badges`                                                               | Spec 313 U1 thin redirect (moved with its parent).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | (redirect)                                   |
| `/sa/plan`               | requireRole([site_admin, super_admin]) — inline literal                                 | Reached: SaTools ปิดวัน tile on /sa home; DailyPlanBoard project switcher (`/sa/plan?project=&date=`); project detail แผนพรุ่งนี้ link. แผนพรุ่งนี้ next-day board (DailyPlanBoard): project + date pickers, add non-complete งานย่อย from picker, assign crew per item, plus แนะนำแผนพรุ่งนี้ draft suggestions. (Spec 273 U2 / ADR 0076 + 281 U2 + 292 U3. ?project= is a view-only override validated vs the visible list; empty visible projects → EmptyNotice ยังไม่มีโครงการที่ดูแล.)                                                                                                                              | chip → `/sa` (หน้าหลัก)                      |
| `/sa/registrations`      | requireRole([site_admin]) — inline literal (site_admin ONLY)                            | Reached: /sa home คำขอสมัครรอตรวจ card (when pending>0); /team CrewProgressRoster registrationsHref. Read-only pending-queue: RegistrationQueueList of technician self-registrations, each row deep-links to detail; no approve/reject affordance. (Spec 263 U3. RLS can_see_technician_registration narrows to pending rows for site_admin; the decide queue is /registrations.)                                                                                                                                                                                                                                        | chip → `/sa` (กลับไปหน้าหลัก)                |
| `/sa/registrations/[id]` | requireRole([site_admin]) — inline literal (site_admin ONLY)                            | Reached: row link from the /sa/registrations list. Read-only registration detail: EmployeeCard, ข้อมูลผู้สมัคร (phone/DOB/submitted-at), RegistrationDocumentsView; no approve/reject. (Spec 263 U3. Invalid UUID or non-pending/not-visible id → notFound(), even if visible at /registrations.)                                                                                                                                                                                                                                                                                                                        | chip → `/sa/registrations` (กลับไปคำขอสมัคร) |

## Project surfaces

| Route                                                 | Gate                                                                                                           | Rows / actions →                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Back →                                                                             |
| ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| `/projects`                                           | PROJECT_VIEW_ROLES (site_admin + PM + super + procurement + proc_mgr + project_coordinator + project_director) | Filterable project-card list (status + client chips, ?q search); PM/super/director also get NewProjectSheet (create project). (Role only decides chrome — kicker + HubNav set branch SA/PM/procurement/coordinator; the query is one shared list.) Procurement tiers reach it via the /procurement ขอบเขต › โครงการ door (spec 326).                                                                                                                                                                                                                                                                                                     | hub/tab (no chip)                                                                  |
| `/projects/[projectId]`                               | PROJECT_VIEW_ROLES                                                                                             | Reached: project rows on /projects hub + dashboard project cards + SA tools/home; WorkPackageList group rows link here. รายการงาน WP list; header chips ⓘ / schedule / supply-plan / store / rentals / reports / settings (each role-gated); PM gets onboarding + import/copy/add-WP; SA/super get แผนพรุ่งนี้. (Non-member gets NoAccessNotice via admin exists-check, not 404. Chip visibility: schedule=SCHEDULE_VIEW_ROLES, supply-plan=SUPPLY_PLAN_ROLES, store=WP_DETAIL_ROLES, rentals=BACK_OFFICE_ROLES, reports+settings=isManagerRole; WP-row open=WP_DETAIL_ROLES, coordinator rows inert.)                                   | chip → ?from ▸ `/projects`                                                         |
| `/projects/[projectId]/deliverables/[deliverableId]`  | PROJECT_VIEW_ROLES                                                                                             | Reached: DeliverablesManager rows on the project settings page (#deliverables). Lists the งาน (WPs) in this งวด; PM/super/director get EditDeliverableSheet + RemoveWorkPackagesSheet / DeleteDeliverableButton (when empty). (Spec 165 U3. project_id pinned in query so a mismatched pair 404s; WP rows link out via withBackFrom → this page.)                                                                                                                                                                                                                                                                                        | chip → `/projects/[projectId]/settings#deliverables` (fixed, กลับไปตั้งค่าโครงการ) |
| `/projects/[projectId]/rentals`                       | BACK_OFFICE_ROLES (PM + super + procurement + proc_mgr + project_director — NOT site_admin)                    | Reached: เช่าอุปกรณ์ (Forklift) chip in the project header (BACK_OFFICE_ROLES only). Project-locked RentalManager — record + list THIS project's equipment rentals (auto-allocated to the project). (Spec 275 U5 money page; the two rental money tables read via admin client, project/supplier names via RLS client; blacklisted suppliers filtered out.)                                                                                                                                                                                                                                                                              | chip → `/projects/[projectId]` (กลับไปโครงการ)                                     |
| `/projects/[projectId]/reports`                       | PM_ROLES (project_manager + super_admin + project_director)                                                    | Reached: รายงาน (FileText) chip in the project header (isManagerRole only). GenerateReportButton (queues a PDF, disabled when one is in-flight) + newest-first ReportsList (status pill + Download when complete, auto-poll). (export maxDuration=60 for the in-request PDF build; generate server action re-checks auth.)                                                                                                                                                                                                                                                                                                               | chip → `/projects/[projectId]` (กลับไปหน้าโครงการ)                                 |
| `/projects/[projectId]/schedule`                      | SCHEDULE_VIEW_ROLES (site_admin + PM + super + project_director + procurement + proc_mgr)                      | Reached: ตารางงาน (CalendarDays) chip in the project header (project_coordinator excluded). Read-only ScheduleViews — WP swimlanes grouped by งวด, dependency arrows, activity spans / critical path. (Spec 173 U2 — procurement reads the Gantt via its U1 RLS arm.)                                                                                                                                                                                                                                                                                                                                                                    | chip → `/projects/[projectId]` (กลับไปโครงการ)                                     |
| `/projects/[projectId]/settings`                      | PM_ROLES (project_manager + super_admin + project_director)                                                    | Reached: ตั้งค่าโครงการ (Settings) chip in the project header (isManagerRole only). SettingsForm (name/status/notes/address/lead/budget/type); ProjectPrimarySiteAdmins (set primary SA); DeliverablesManager (งวดงาน) + CategoriesManager (หมวดงาน) on open projects; client-portal invite + grant-existing. (SA never lands, requireRole redirect. budget + staff/client-binding read via admin client. Client-portal issue gated CLIENT_ISSUER_ROLES (project_director + super_admin only) — PM reaches page but can't issue.)                                                                                                        | chip → `/projects/[projectId]` (กลับไปรายการงาน)                                   |
| `/projects/[projectId]/store`                         | WP_DETAIL_ROLES (site_admin + PM + super + project_director + procurement + proc_mgr)                          | Reached: คลัง (Warehouse) chip in the project header (admits site_admin the storekeeper). StoreManager (on-hand list → item log, รับเข้า receive); DivertToStoreList; ตรวจนับ StoreCountManager + เบิก gated to canIssue=SITE_STAFF_ROLES; StorePnlView only for super_admin/project_director. (Spec 197 U1. Procurement reaches page read-only. P&L + PR/divert reads scoped by canIssue/canSeePnl; empty-state supply-plan link only when canPlanSupply.)                                                                                                                                                                              | chip → `/projects/[projectId]` (กลับไปโครงการ)                                     |
| `/projects/[projectId]/store/items/[catalogItemId]`   | WP_DETAIL_ROLES                                                                                                | Reached: tap an on-hand row in StoreManager (store page) — storeItemHref via withBackFrom. ประวัติวัสดุ — current on-hand (qty + avg/total cost) + newest-first movement timeline (รับเข้า/เบิก/ตรวจนับ/คืน/แก้รายการ) merged from the 5 append-only tables. (Spec 213 U2 — cost-side only, no sell/margin. 404 if the item has neither an on-hand row nor any movement in this project.)                                                                                                                                                                                                                                                | chip → ?from ▸ `/projects/[projectId]/store` (กลับไปคลัง)                          |
| `/projects/[projectId]/supply-plan`                   | SUPPLY_PLAN_ROLES (PM + super + project_director + procurement + proc_mgr)                                     | Reached: แผนจัดหา (ClipboardList) chip in the project header; the /procurement hub's ขอบเขต › แผนจัดหา 📍 door (#621, lens-active/sole project); store empty-state link. Lists the project's plans (NewPlanButton, CloneTemplateButton, DeletePlanButton for draft/rejected); ?plan=<id> opens SupplyPlanManager (WP-scoped catalog picker); SupplyPlanAccuracy card planner-only. (Spec 176/189/181. isPlanner = role!=='procurement' (accuracy hidden for procurement); canApprove = project_director/super_admin, canOverride = super_admin.)                                                                                         | chip → `/projects/[projectId]` (กลับไปโครงการ)                                     |
| `/projects/[projectId]/work-packages/[workPackageId]` | WP_DETAIL_ROLES                                                                                                | Reached: WP rows on the project WorkPackageList, deliverable-detail list, /sa home, schedule, and purchase-request detail — all via withBackFrom. Group WP → GroupDetailView (children + money rollup). Leaf WP → PhaseProgressBar + tabs รูปถ่าย / คำขอซื้อ / เบิกของ / ค่าใช้จ่ายหน้างาน / ทีมงาน / อุปกรณ์ / ข้อมูล / จัดการ; plus submit-for-approval / report-defect / contractor-assign. (Spec 171/167 — procurement is read-only (isReadOnlyWpViewer): PR-raise only, capture/issue/expense/manage suppressed. isPlanner (isManagerRole) adds จัดการ tab + labor-budget money; group money via admin client behind planner gate.) | chip → ?from ▸ `/projects/[projectId]` (กลับ)                                      |

## Purchasing surfaces

`PURCHASING_ROLES` = site_admin/pm/super + procurement + procurement_manager +
project_director. Procurement/proc_mgr get the buyer pipeline; other roles get
the requester card list. **PRs are created on the WP page, never here.** The
report/register surfaces widen to `PURCHASE_REPORT_ROLES` (adds accounting,
drops site_admin); PO list + detail widen to `PO_DETAIL_VIEW_ROLES`
(`PURCHASING_ROLES` + accounting).

| Route                                             | Gate                                                                                                                                       | Rows / actions →                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Back →                                    |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- |
| `/requests`                                       | PURCHASING_ROLES (site_admin/pm/super + procurement + procurement_manager + project_director)                                              | THE purchasing worklist: procurement/proc_mgr see the buyer pipeline (KPI hero, status-chip bands, supplier/project filters, desktop grid + phone PO-basket/PO cards), everyone else the requester card list (view active/done/all + เฉพาะของฉัน chips). (Role-variant: isProcurementWorklist forks buyer-pipeline vs site card-band list. HubNav strip via hubNavForRole; no back-bar.)                                                                                                                                                                                                        | hub/tab (no chip)                         |
| `/requests/[requestId]`                           | PURCHASING_ROLES                                                                                                                           | Reached: worklist cards on /requests, the procurement grid drawer, PO-detail member lines, and delivery-detail lines (each via withBackFrom ?from). PR detail: quantity/notes, requisition tracker, decision (approve/reject), price-comparison → create-PO, ship, cancel, site-purchase PM-ack, and reference/invoice/payment/delivery-confirmation galleries + uploaders. (notFound() on non-UUID / unreadable id. Decision gated isPurchaseDecider (requested→decided); site-purchase ack isManagerRole; cancel(approved) isProcurementManagerTier; back-office sees quotes/create-PO/ship.) | chip → ?from ▸ `/requests`                |
| `/requests/orders`                                | PO_DETAIL_VIEW_ROLES (PURCHASING_ROLES + accounting)                                                                                       | Reached: the /procurement hub's เวลา › ใบสั่งซื้อ door (spec 323 U3b; the old procurement รายงาน tab that lit here is gone) + procurement home tiles on /requests/reports (PO ค้างส่ง ?pending=1 · ค้างรับเข้า). PO list (ตรวจสอบย้อนหลัง): `<ProjectLens>` chips + date/supplier/project GET filter (spec 323 U4), one row per PO with derived status, charges-aware grand total, aging (undelivered) — rows link to PO detail. (Spec 262 U3. Reads via admin client (loadPurchaseOrderList); no schema-level list gate beyond the route.)                                                     | chip → `/requests` (งานจัดซื้อ)           |
| `/requests/orders/[poId]`                         | PO_DETAIL_VIEW_ROLES (PURCHASING_ROLES + accounting)                                                                                       | Reached: PR-detail อยู่ใน-PO link, procurement-grid PO header, PoGroupCard, orders-list rows, register PO tags, and the accounting voucher (?from) — all via withBackFrom. PO detail as a unit: void (mgr only), roll-up line count + grand total (money), deliveries section (manage = back-office), รับของ receive checklist (RECEIVE_ROLES), member-ticket list, PO-level charges (money). (notFound() on non-UUID / unreadable id. accounting reads via admin client and lands from a voucher; money hidden from site_admin (canSeeMoney); void = isProcurementManagerTier.)                | chip → ?from ▸ `/requests`                |
| `/requests/orders/[poId]/deliveries/[deliveryId]` | PURCHASING_ROLES                                                                                                                           | Reached: the การจัดส่ง (งวดส่ง) list in the PO detail's PoDeliverySection (deliveryDetailHref). Delivery (งวด) detail: eta / cost[back-office] / note, dispatch control (back-office), proof-of-delivery gallery + uploader, งวด member lines, and ย้ายเข้าคลัง divert-to-store (SITE_STAFF only). (notFound() on non-UUID or a delivery belonging to another PO. Divert list gated SITE_STAFF_ROLES; procurement reaches page but is read-only in store.)                                                                                                                                      | chip → PO detail (poDetailHref, no ?from) |
| `/requests/reports`                               | PURCHASE_REPORT_ROLES (procurement + procurement_manager + project_manager + project_director + super_admin + accounting — NOT site_admin) | Reached: the /procurement hub's เวลา › รายงาน door (spec 323 U3b; was the procurement รายงาน tab); procurement home tiles (เดือนนี้สั่งซื้อ → ?preset=month). Purchase report (spec 262 U2): procurement home tiles, period presets + custom-range/project GET form, bucket & group-by chips, totals/budget/trend strips, detail rows drilling to the register, CSV download link. (Calls purchase_report RPC via session server client, which self-gates on current_user_role. group-by ผู้สั่งซื้อ only for isProcurementManagerTier; budget strip via admin client.)                         | chip → `/requests` (งานจัดซื้อ)           |
| `/requests/reports/register`                      | PURCHASE_REPORT_ROLES (— NOT site_admin)                                                                                                   | Reached: drill from a /requests/reports detail row (registerDrillHref — a bucket×dimension slice). Register-style purchase list for that slice: summary total, rows grouped by PO (PO tag links to order detail via ?from); read-only — no filter form (change the slice on the report). (Spec 262 U2. Rows are terminal — no per-row drill, no single destination admits all 6 report roles. Reuses /accounting/purchases loader + view; admin-client reads.)                                                                                                                                  | chip → `/requests/reports` (รายงาน)       |

## Review, dashboard and staff surfaces

| Route                                   | Gate                                                                                                          | Rows / actions →                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Back →                                    |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- |
| `/review`                               | PM_ROLES (project_manager/super_admin/project_director)                                                       | Reached: the /dashboard รอตรวจ card (PendingApprovalsCard → /review); the ภาพรวม tab/strip lights on it but points to /dashboard. Lists work_packages in status pending_approval oldest-waiting first, each a card linking to its review screen with a decision-status pill (รอตรวจครั้งแรก / ตีกลับ). (Spec 183 U2: the queue is a sub-surface of ภาพรวม, HubNav currentHref=/dashboard; approved WPs go complete and drop off.)                                                                                                                                            | hub/tab (no chip)                         |
| `/review/work-packages/[workPackageId]` | PM_ROLES                                                                                                      | Reached: from the /review queue cards. WP review screen: inline สร้างคำขอซื้อ PR form (self-approve), photo galleries (defect→fix pairs + phases + after-fix rounds), บันทึกทีมงานรายวัน labor zone, ค่าแรง budget+cost, ประวัติการตรวจ, and บันทึกผลการตรวจ decision form + hold toggle. (Spec 54/136/248; RecordDecisionForm renders only when status=pending_approval; money via admin client behind the gate; notFound on missing WP/project.)                                                                                                                           | chip → `/review` (กลับไปรายการรอตรวจ)     |
| `/dashboard`                            | DASHBOARD_VIEW_ROLES (SITE_STAFF_ROLES: site_admin/project_manager/super_admin/project_director + accounting) | Role-aware portfolio: pending-approval hero + bank-change awareness card (PM tier only), งบประมาณรวม + spend-by-หมวดงาน (money-view set), live project cards linking to /projects/[id]. (Spec 100; money display = MONEY_VIEW_ROLES (PM tier + accounting), site_admin sees none; approval/bank cards gated to isManager (PM_ROLES); accounting lists via admin client.)                                                                                                                                                                                                     | hub/tab (no chip)                         |
| `/registrations`                        | STAFF_APPROVAL_ROLES (procurement_manager/project_director/super_admin)                                       | Reached: PM hub strip คำขอสมัคร (director/super) + REGISTRATIONS_TAB bottom bar (PM set) + the /procurement hub's คำขอสมัคร nudge (proc_mgr, spec 323 U3a/U3b). Staff self-registration approval queue (role-neutral); rows link to /registrations/[id]; empty = ไม่มีคำขอสมัคร. (Spec 263 U3 / 264 G4; route gate === page gate === approve_staff_registration RPC's inline literal.)                                                                                                                                                                                       | chip → `/dashboard` (กลับไปหน้าภาพรวม)    |
| `/registrations/[id]`                   | STAFF_APPROVAL_ROLES                                                                                          | Reached: from the /registrations queue rows. Applicant review detail: EmployeeCard, LINE-identity block, applicant fields (+inviter), uploaded docs (signed URLs), and approve/reject with role selector + active-project site picker (RegistrationDecision, pending only). (Spec 263/264/265 U2 / 279 F2b; notFound on invalid uuid or unresolved row; LINE identity + inviter via admin client; decision UI only when status=pending.)                                                                                                                                     | chip → `/registrations` (กลับไปคำขอสมัคร) |
| `/workers`                              | WORKER_ROSTER_ROLES (PM_ROLES + procurement + procurement_manager)                                            | Reached: PM hub strip ทีมงาน + the /procurement hub's ทรัพยากร › รายชื่อช่าง door (spec 323 U3b; was on the procurement strips) + /settings รายชื่อช่าง card (ทีมช่าง survives U4 for the procurement tiers); settings tab lights on it (non-procurement — spec 323 U4 unlit-leaf rule). ช่าง roster manager: create/edit/assign workers with day_rate + level, หัวหน้าช่าง badge; super_admin also gets a Nova sell-rate table link. (Spec 46 P1 / 172 Phase C; day_rate via admin client (page gate authorizes); role-variant canGrade=super_admin, canAssignHt=PM_ROLES.) | chip → `/settings` (ตั้งค่า)              |
| `/payroll`                              | PAYROLL_VIEW_ROLES (PAYROLL_ROLES: PM_ROLES + procurement + procurement_manager, + accounting)                | Reached: /settings ค่าแรง card + the /procurement hub's ทรัพยากร › ค่าแรง door (spec 323 U3b; spec 309's procurement tab/strip items collapsed into it); settings tab lights on it (non-procurement — spec 323 U4 unlit-leaf rule). ค่าแรง per-worker roll-up for a period (zero-JS GET date form), summary card + per-worker paid/ค้างจ่าย status, ดาวน์โหลด CSV link, and RecordPaymentSheet. (Spec 69/170 U3 / 266 U4; canRecord=PAYROLL_ROLES so accounting is read-only (sheet hidden, record_wage_payment RPC refuses it); money via admin client.)                    | chip → `/settings` (ตั้งค่า)              |

## Settings hub and reference data

| Route                                       | Gate                                                                                                                                                                                           | Rows / actions →                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Back →                                                           |
| ------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| `/settings`                                 | session-only (getClaims; redirect /login if no claims or users row) — NOT requireRole, so unserved roles aren't bounced; every authenticated role                                              | บัญชี (→/profile), config sections by role (field/ข้อมูลหลัก/การเงิน/ความช่วยเหลือ/เร็วๆนี้/ผู้ดูแลระบบ from sections.ts; spec 323 U4 — the procurement tiers see NO ข้อมูลหลัก/ค่าใช้จ่าย sections, those doors live on their /procurement hub), theme toggle, super_admin daily-report test, version. (super_admin gets a feedback triage badge + daily-report preview; the role→entry matrix lives in ./sections.ts.)                                                                                          | hub/tab (no chip)                                                |
| `/settings/friction-map`                    | requireRole(["super_admin"])                                                                                                                                                                   | Reached: /settings ผู้ดูแลระบบ section (จุดสะดุดรายจอ). Ranks screens by friction-event count (js_error/upload_fail/validation_error/form_abandon/rage_tap) over the last 14 days, all users. (spec 244 U4 / ADR 0068; RLS-scoped read.)                                                                                                                                                                                                                                                                          | chip → `/settings` (กลับไปตั้งค่า)                               |
| `/settings/integrity`                       | requireRole(["super_admin"])                                                                                                                                                                   | Reached: /settings ผู้ดูแลระบบ section (ตรวจระบบ). run_integrity_checks board grouped by domain (money/access/identity/schema) with red/amber/green/na tiles + ตรวจเดี๋ยวนี้ run-now form. (spec 283 U1; read-only, hourly cron records history.)                                                                                                                                                                                                                                                                 | chip → `/settings` (กลับไปตั้งค่า)                               |
| `/settings/ordering-templates`              | SUPPLY_PLAN_ROLES (pm/super/director + procurement + proc_mgr)                                                                                                                                 | Reached: /settings ข้อมูลหลัก section (non-procurement) + the /procurement hub's ขอบเขต › เทมเพลตแผนจัดหา door (spec 323 U4). Lists the seeded global supply_plans (is_template=true); each row opens its stripped editor. No create-new UI. (spec 245 U4.)                                                                                                                                                                                                                                                       | chip → `/settings` (กลับไปตั้งค่า)                               |
| `/settings/ordering-templates/[templateId]` | SUPPLY_PLAN_ROLES                                                                                                                                                                              | Reached: ordering-templates list rows. OrderingTemplateEditor — item + qty + note rows (shared catalog picker with thumbnails / managed categories). (spec 245 U4; notFound() if id isn't a real is_template row.)                                                                                                                                                                                                                                                                                                | chip → `/settings/ordering-templates` (ORDERING_TEMPLATES_LABEL) |
| `/settings/org-chart`                       | requireRole(["super_admin"])                                                                                                                                                                   | Reached: /settings ผู้ดูแลระบบ section (โครงสร้างองค์กร). Read-only: each active department → head (departments.head_user_id) → members (users grouped by department_id). (spec 284 U2 / ADR 0080; assignment via super_admin RPCs, no UI here.)                                                                                                                                                                                                                                                                  | chip → `/settings` (กลับไปตั้งค่า)                               |
| `/settings/roles`                           | requireRole(["super_admin"])                                                                                                                                                                   | Reached: /settings ผู้ดูแลระบบ section (จัดการสิทธิ์ผู้ใช้). Lists all users grouped by role (visitors/รอกำหนดสิทธิ์ first); change role via gated set_user_role RPC in RoleAdminList. (spec 220 / ADR 0050; RLS super_admin full-access read.)                                                                                                                                                                                                                                                                   | chip → `/settings` (กลับไปตั้งค่า)                               |
| `/settings/roles/[id]`                      | requireRole(["super_admin"]) (notFound on invalid uuid / missing user)                                                                                                                         | Reached: /settings/roles user rows. Shows app name + role beside the LINE ground-truth identity block (display name, original avatar, last synced) for drift/impersonation check. (spec 265 U2; read-only.)                                                                                                                                                                                                                                                                                                       | chip → `/settings/roles` (กลับไปจัดการสิทธิ์ผู้ใช้)              |
| `/settings/usage`                           | requireRole(["super_admin"])                                                                                                                                                                   | Reached: /settings ผู้ดูแลระบบ section (การใช้งานแอป). Per-user DAU + screen-time + friction over 14 days from usage_daily (internal roles only, sorted by name); each person links to their timeline. (spec 244 U1b/U1c; protective framing, not a scoreboard.)                                                                                                                                                                                                                                                  | chip → `/settings` (กลับไปตั้งค่า)                               |
| `/settings/usage/[actorId]`                 | requireRole(["super_admin"]) (notFound on missing / external-role user)                                                                                                                        | Reached: /settings/usage per-person rows. One person's 14-day day-grouped session timeline (start time, duration, screens in order, inline friction) via get_actor_timeline RPC. (spec 244 U5; throws to the error boundary on transient read fail — never a false 'no activity'.)                                                                                                                                                                                                                                | chip → `/settings/usage` (กลับไปการใช้งานแอป)                    |
| `/settings/view-as`                         | session-only → super_admin check (getClaims + users.role; redirect /login if no claims, redirect / if not super) — deliberately NOT requireRole (would bounce a super already assuming a role) | Reached: /settings ผู้ดูแลระบบ section (ดูมุมมองตาม role). Grid of ASSUMABLE_ROLES forms posting setAssumedRole (lands on that role's home); active view highlighted with ออกจากมุมมอง exit. (spec 274 U2 / ADR 0077; own super_admin BottomTabBar shown even while assumed.)                                                                                                                                                                                                                                     | chip → `/settings` (กลับไปตั้งค่า)                               |
| `/settings/wp-grouping-import`              | requireRole(["super_admin"])                                                                                                                                                                   | Reached: URL only — no nav card yet (comment: settings-hub card arrives with U3 roster work). Pick project → download pre-filled template → paste completed TSV → dry-run → apply via import_wp_grouping RPC (GroupingImportForm). (spec 270 U2b; งาน/งานย่อย rename+renumber import.)                                                                                                                                                                                                                            | chip → `/settings` (กลับไปตั้งค่า)                               |
| `/catalog`                                  | BACK_OFFICE_ROLES (pm/super/director + procurement + proc_mgr)                                                                                                                                 | Reached: /settings ข้อมูลหลัก section (non-procurement) + the /procurement hub's ขอบเขต › แคตตาล็อก door (the procurement tiers' path since spec 323 U4). Item master list + AddCatalogItem + CatalogList edit; link to จัดการหมวดหมู่ (→/catalog/subcategories); sell-rate read/edit for super_admin+director only (admin client). (spec 175 U1; dormant BOQ-template link retired, hide-not-drop.)                                                                                                              | chip → `/settings` (ตั้งค่า)                                     |
| `/catalog/boq-templates`                    | BACK_OFFICE_ROLES                                                                                                                                                                              | Reached: URL only — the /catalog entry link was retired (spec 239 U2, hide-not-drop). Firm-wide BOQ template list (active first, inactive muted); AddBoqTemplate sheet (create_boq_template); each links to detail. (spec 237 / ADR 0066 S10-U2.)                                                                                                                                                                                                                                                                 | chip → `/catalog` (CATALOG_LABEL)                                |
| `/catalog/boq-templates/[templateId]`       | BACK_OFFICE_ROLES                                                                                                                                                                              | Reached: /catalog/boq-templates list rows. BoqTemplateDetail — template + lines + line form (catalog / categories / units / work-category picker data). (spec 237; notFound() if template missing/unseen.)                                                                                                                                                                                                                                                                                                        | chip → `/catalog/boq-templates` (BOQ_TEMPLATES_LABEL)            |
| `/catalog/subcategories`                    | BACK_OFFICE_ROLES                                                                                                                                                                              | Reached: /catalog จัดการหมวดหมู่ (MANAGE_TAXONOMY_LABEL) link. Manage main catalog_categories (Add/EditCategory — recode/rename/deactivate) + subcategories grouped under the 13 enum categories (Add/EditSubcategory). (spec 219 U2 / 221 U3.)                                                                                                                                                                                                                                                                   | chip → `/catalog` (CATALOG_LABEL)                                |
| `/equipment`                                | EQUIPMENT_MOVE_ROLES (sa + pm/super/director + procurement + proc_mgr)                                                                                                                         | Reached: /settings หน้างาน section (site_admin) + /settings ข้อมูลหลัก section (back-office minus procurement) + the /procurement hub's ทรัพยากร › อุปกรณ์ door (procurement tiers since spec 323 U4) — อุปกรณ์. Equipment registry list + record movement (EquipmentManager); back-office (BACK_OFFICE_ROLES) also edits registry, sees daily-rate money (admin client) + rental link. (spec 141 U2/U5; site_admin = field view only, no registry edit / no rates.)                                              | chip → `/settings` (ตั้งค่า)                                     |
| `/equipment/rentals`                        | BACK_OFFICE_ROLES (pm/super/director + procurement + proc_mgr)                                                                                                                                 | Reached: /settings ข้อมูลหลัก section (non-procurement) + the /procurement hub's ทรัพยากร › เช่าอุปกรณ์ door (spec 323 U4) + back-office-only link on /equipment. Read-only list of inbound rental batches + settlements; FAB→sheet forms for deal/settlement, per-card allocate/void/correct sheets, settlement receipt uploads (spec 323 U1c/U1d; the variance card retired in U1a). Carries `?project=` via the hub door. (spec 268 / 275; money surface — zero-grant tables via admin client after the gate.) | chip → `/equipment` (อุปกรณ์)                                    |

## Contacts

| Route                      | Gate                                                                                                                                                    | Rows / actions →                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Back →                                                                                                |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------- |
| `/contacts/[type]/[id]`    | requireRole(BACK_OFFICE_TYPES.has(type) ? BACK_OFFICE_ROLES : PM_ROLES) — contractors/suppliers: BACK_OFFICE_ROLES; clients/service-providers: PM_ROLES | Reached: row tap from the owning contact list tab (contacts-tabs on /contacts/customers · /contacts/vendors · /contacts/subcontractors), via withBackFrom. Read-only contact fields + money-isolated bank block; supplier ยอดซื้อตามหมวด spend; สถานะเอกสาร DC packet; pending bank-change decisions, documents, portal invite, consent, crew (contractors). (Spec 88 U5. notFound() on unknown type or missing record. Procurement gets read+bank only — the write blocks (bank-change, docs, invite, consent) render for isPmTier only.) | chip → ?from ▸ contactListPath(type) (safeBackHref, e.g. /contacts/subcontractors, /contacts/vendors) |
| `/contacts/bank-changes`   | PM_ROLES (pm/super/director)                                                                                                                            | Reached: dashboard bank-change awareness card. Merged contractor + worker pending bank-change queue, oldest-first, each with inline อนุมัติ/ปฏิเสธ via BankChangeDecision. (Spec 186 U1. Bank fields are money (zero grant) → admin-read behind the gate; procurement excluded.)                                                                                                                                                                                                                                                           | chip → `/dashboard`                                                                                   |
| `/contacts/customers`      | PM_ROLES (pm/super/director)                                                                                                                            | Reached: settings hub ข้อมูลหลัก → ลูกค้า (manager-only entry) + HubNav strip. ลูกค้า (clients) list via ContactsTabs — inline add/edit of name/contact/phone/email/address/note. (Spec 99. User-session read, no money column; bank lives admin-only in contact_bank.)                                                                                                                                                                                                                                                                    | chip → `/settings`                                                                                    |
| `/contacts/subcontractors` | BACK_OFFICE_ROLES (pm/super/director + procurement + proc_mgr)                                                                                          | Reached: settings hub ข้อมูลหลัก → ผู้รับเหมาช่วง (non-procurement since spec 323 U4; the tiers use the hub's ทรัพยากร door) + HubNav strip. ผู้รับเหมาช่วง list (contractors where contractor_category='contractor') via ContactsTabs — inline edit. (Spec 168. Procurement admitted as back-office curator, spec 172 Phase B.)                                                                                                                                                                                                           | chip → `/settings`                                                                                    |
| `/contacts/vendors`        | BACK_OFFICE_ROLES (pm/super/director + procurement + proc_mgr)                                                                                          | Reached: settings hub ข้อมูลหลัก → ผู้ขาย/ผู้ให้บริการ + HubNav; the /procurement hub's ทรัพยากร › ผู้ขาย door (spec 323 U3b; was a procurement bottom-tab). Managers: ผู้ขาย + ผู้ให้บริการ tabs with detail links. Procurement: suppliers-only, no detail links, per-supplier spend badges. (Spec 99/101. Role-variant title/back/tabs; service_providers PM-only, procurement RLS excludes them.)                                                                                                                                       | chip → `/settings` (managers) or `/requests` (procurement) — role-variant                             |

## Accounting

| Route                              | Gate                                                | Rows / actions →                                                                                                                                                                                                                                                                                                                                                                                                                                          | Back →                                                                                                                             |
| ---------------------------------- | --------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `/accounting`                      | ACCOUNTING_ROLES (accounting + super_admin)         | Reached: accounting bottom-tab บัญชี; roleHome for accounting; settings hub การเงิน → บัญชี + HubNav. การกระทบยอด reconciliation checks; ทะเบียน register nav (projects/billings/retention/wht/payables/purchases/periods); manager-only สมุดรายวัน card; period GET form; P&L; trial-balance rows drill to /accounting/ledger. (Spec 149 U9. สมุดรายวันทั่วไป link shown only to isManagerRole (super_admin here); period/project via GET searchParams.) | chip → `/settings`                                                                                                                 |
| `/accounting/billings`             | ACCOUNTING_ROLES (accounting + super_admin)         | Reached: /accounting ทะเบียน nav → งวดงาน. Client-billing (งวด) register; writers create draft claim, รับรองงวด (books AR/revenue/VAT/WHT + retention), วางบิล, record receipts. (Spec 149 U9b + 204. Write controls only for BILLING_WRITE_ROLES (super_admin overlaps in beta); accounting read-only.)                                                                                                                                                  | chip → `/accounting`                                                                                                               |
| `/accounting/journal`              | PM_ROLES (pm/super/director) — NOT ACCOUNTING_ROLES | Reached: /accounting manager-only card สมุดรายวันทั่วไป (isManagerRole). Manual general-journal POST form (ManualJournalForm) + reverse over post_journal_entry/reverse_journal_entry RPCs; recent-entries list. (Spec G8. POST-capability gate deliberately distinct from the accounting VIEW gate; only super_admin overlaps both.)                                                                                                                     | chip → `/accounting`                                                                                                               |
| `/accounting/ledger`               | ACCOUNTING_ROLES (accounting + super_admin)         | Reached: /accounting trial-balance account rows (ledgerHref carrying code/from/to/project) + /accounting/payables supplier rows. Every posted GL line hitting one account over the period, with source doc + counterparty + debit/credit/net totals — the vouching primitive. (Spec 196 Tier 1. Supplier-scoped drill defaults the window to full history (AP statement); account drill keeps its period.)                                                | chip → `/accounting/payables` (supplier AP drill) or `/accounting?from&to&project` (trial-balance drill) — bespoke, scope-computed |
| `/accounting/payables`             | ACCOUNTING_ROLES (accounting + super_admin)         | Reached: /accounting ทะเบียน nav → เจ้าหนี้การค้า. AP subledger — supplier roll-up behind the 2100 control total; each real supplier drills to its ledger AP statement (null-counterparty bucket does not link). (Spec 196 Tier 2.)                                                                                                                                                                                                                       | chip → `/accounting`                                                                                                               |
| `/accounting/periods`              | ACCOUNTING_ROLES (accounting + super_admin)         | Reached: /accounting ทะเบียน nav → ปิดงวดบัญชี. เปิดงวดเดือนนี้; per-period transitions open→closing→closed (+ super_admin reopen/lock), each a ConfirmActionButton; posting-backlog pre-close guard. (Spec 196 Tier 4. closed→open / closed→locked transitions gated to super_admin (canTransitionPeriod isSuper).)                                                                                                                                      | chip → `/accounting`                                                                                                               |
| `/accounting/projects`             | MONEY_VIEW_ROLES (pm/super/director + accounting)   | Reached: /accounting ทะเบียน nav → การเงินรายโครงการ. Project list; each row shows วางบิล/รับแล้ว/ค้างรับ funnel headline and links into the per-project drill. (Spec 253 U1. Wider gate than the sibling registers (MONEY_VIEW_ROLES, spec 252 posture) — admits PM tier too, not just ACCOUNTING_ROLES.)                                                                                                                                                | chip → `/accounting`                                                                                                               |
| `/accounting/projects/[projectId]` | MONEY_VIEW_ROLES (pm/super/director + accounting)   | Reached: row tap from /accounting/projects. Per-project revenue funnel (quotation→PO→สัญญา/งวดเบิก→billed→received incl. advances), ต้นทุน (labor/material split), กำไร-ขาดทุน per-WP table; PM-tier write sheets (quotation/PO/contract/installment/advance). (Spec 253 U1. notFound() on missing project; write sheets render for PM_ROLES only (canWrite), wp_profit runs on user session.)                                                            | chip → `/accounting/projects`                                                                                                      |
| `/accounting/purchases`            | ACCOUNTING_ROLES (accounting + super_admin)         | Reached: /accounting ทะเบียน nav → การจัดซื้อ. Purchase register grouped by PO (direct buys last) with period+project GET filter; gross/VAT/net period totals; rows drill to voucher. (Spec 196 Tier 3 U5. Period/project via searchParams.)                                                                                                                                                                                                              | chip → `/accounting`                                                                                                               |
| `/accounting/purchases/[id]`       | ACCOUNTING_ROLES (accounting + super_admin)         | Reached: row tap from /accounting/purchases. Purchase voucher — supplier + VAT breakdown, doc-fact meta, เอกสารประกอบ source attachments (invoice/delivery/payment), and การลงบัญชี GL entry it posted. (Spec 196 Tier 3 U6. notFound() on missing header; PO meta row links to /requests/orders/[poId] via withBackFrom, PO_DETAIL_VIEW_ROLES admits accounting.)                                                                                        | chip → `/accounting/purchases`                                                                                                     |
| `/accounting/retention`            | ACCOUNTING_ROLES (accounting + super_admin)         | Reached: /accounting ทะเบียน nav → เงินประกันผลงาน. Retention register by status (ถือไว้/ครบกำหนด/คืนแล้ว/ริบ) with open-balance headline; writers mark due / release via RetentionRowActions. (Spec 149 U9b + 204. Row actions only for BILLING_WRITE_ROLES (super_admin in beta); accounting read-only.)                                                                                                                                                | chip → `/accounting`                                                                                                               |
| `/accounting/wht`                  | ACCOUNTING_ROLES (accounting + super_admin)         | Reached: /accounting ทะเบียน nav → ภาษีหัก ณ ที่จ่าย. WHT certificate register (ภ.ง.ด.3/53/1; เราหัก deducted / ถูกหัก suffered) with rate + base; managers record a new WHT via RecordWhtForm. (Spec 149 U9b. Record form shown only to isManagerRole — super_admin among ACCOUNTING_ROLES reachers.)                                                                                                                                                    | chip → `/accounting`                                                                                                               |

## Feedback

| Route              | Gate                                                                                             | Rows / actions →                                                                                                                                                                                                                                                                                                                                                                                                 | Back →                                                       |
| ------------------ | ------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| `/feedback`        | session-only (getClaims, every authenticated role)                                               | Reached: settings hub 'help' section row (shown to everyone; carries a ตอบกลับใหม่ unread badge). FeedbackForm submit surface (แจ้งปัญหา / ขอฟีเจอร์) + 'เรื่องที่เคยแจ้ง →' link to /feedback/mine. (spec 201 — submit-only surface; tracking split to /feedback/mine.)                                                                                                                                         | chip → `/settings` (กลับไปตั้งค่า)                           |
| `/feedback/[id]`   | session-only (getClaims); RLS scopes the feedback row to submitter-or-super_admin, else notFound | Reached: from /feedback/mine (reporter) and the /feedback/review kanban (operator), passing ?from. Report card + attachment gallery + reply thread; FeedbackReply for the submitter or super_admin; super_admin also sees FeedbackDrafts (approve/discard CC drafts); MarkFeedbackViewed clears the unread dot. (Role-variant: super_admin sees CC drafts and may reply to any report; reporter only their own.) | chip → ?from ▸ `/feedback` (safeBackHref fallback /feedback) |
| `/feedback/mine`   | session-only (getClaims); RLS scopes list to submitted_by = auth.uid()                           | Reached: 'เรื่องที่เคยแจ้ง →' link on /feedback. MyFeedbackList — the caller's own reports (number/type/status/title) with unread-reply dots (feedback_unread_ids RPC), each linking to its conversation. (spec 201 review-kanban split.)                                                                                                                                                                        | chip → `/feedback`                                           |
| `/feedback/review` | requireRole(["super_admin"]) — super_admin only                                                  | Reached: settings hub 'admin' section row (super_admin only; carries a รอตรวจ open-count badge). FeedbackKanban triage board — every report as a card under its lifecycle column (ใหม่→กำลังดำเนินการ→เสร็จ/ปฏิเสธ), status control moves columns, each links to the conversation; header shows total + ใหม่ counts. (spec 193 U3 / spec 201; attachment thumbnails minted via service-role admin.)              | chip → `/settings` (กลับไปตั้งค่า)                           |

## Legal

| Route                           | Gate                                           | Rows / actions →                                                                                                                                                                                                                                                                                                                                                                                                               | Back →                                           |
| ------------------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------ |
| `/legal`                        | requireRole(LEGAL_ROLES) — legal + super_admin | Reached: legal-role bottom tab 'กฎหมาย' (LEGAL_TABS) + legal desktop hub strip (LEGAL_HUB_NAV); roleHome('legal'). LegalHome: active-contracts + pending(draft) counts and two entry cards drilling into /legal/contracts (สัญญา) and /legal/approvals (เอกสารรออนุมัติ). (Spec 284 U5 / ADR 0080; counts read via admin client (contracts is zero-grant); U1 deferred, U5 landed.)                                            | chip → `/settings` (ตั้งค่า)                     |
| `/legal/approvals`              | requireRole(LEGAL_ROLES) — legal + super_admin | Reached: entry card 'เอกสารรออนุมัติ' on /legal (LegalHome). Draft-contract decision queue: each row links to the contract detail and carries a DocumentDecisionForm (approve / reject / needs_revision + required comment → submit_document_decision); approve flips draft→active and leaves the queue. (Spec 284 U5 / ADR 0080; rows read via admin client (zero-grant); row links use withBackFrom(/legal/approvals).)      | chip → `/legal` (ฝ่ายกฎหมาย)                     |
| `/legal/contracts`              | requireRole(LEGAL_ROLES) — legal + super_admin | Reached: entry card 'สัญญา' on /legal (LegalHome). ContractCreateForm (createContract) + status-faceted contract list (chips per CONTRACT_STATUS_LABEL) with rows drilling into the [contractId] detail. (Spec 284 U5 / ADR 0080; rows read via admin client; deep-linkable ?status= facet; row links use withBackFrom(/legal/contracts).)                                                                                     | chip → `/legal` (ฝ่ายกฎหมาย)                     |
| `/legal/contracts/[contractId]` | requireRole(LEGAL_ROLES) — legal + super_admin | Reached: rows in the /legal/contracts list AND the /legal/approvals queue (both via withBackFrom). Deal header (counterparty/type/project/value/dates), current attachments list (superseded_by-null), and ContractVoidButton (voidContract) when status ≠ void. (Spec 284 U5 / ADR 0080; contract + attachments read via admin client; missing contract → notFound(); attachment UPLOAD deferred, Storage bucket = own unit.) | chip → ?from ▸ `/legal/contracts` (safeBackHref) |

## External tiers (portal · client · technician · register)

| Route                           | Gate                                                                                  | Rows / actions →                                                                                                                                                                                                                                                                                                                                                                                                                 | Back →                                                  |
| ------------------------------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| `/client`                       | requireRole(["client"]) — client only (non-client → roleHome)                         | Reached: roleHome('client') post-login landing (LINE callback / login). Lists the client's live projects: 0 → access-ended, 1 → renders that project's ClientProgressView inline, ≥2 → ClientProjectList to drill into /client/[projectId]. (Spec 234 / ADR 0067; 0 projects → redirect /client/access-ended; a super_admin viewing-as client with 0 grants shows ViewAsEmptyNote instead.)                                      | bespoke: sticky header, logout only (no back chip)      |
| `/client/[projectId]`           | requireRole(["client"]) — client only                                                 | Reached: drilled in from the /client project list (ClientProjectList) when the client has ≥2 projects. Read-only ClientProgressView for one project: summary, WP-status list, approved-photo grid, report PDFs; logout only. (Spec 234 U2 / ADR 0067; loadClientView RLS-scoped, null → redirect /client.)                                                                                                                       | bespoke: ← chip → `/client` (ClientProgressView header) |
| `/client/[projectId]/wp/[wpId]` | requireRole(["client"]) — client only                                                 | Reached: WP rows in the ความคืบหน้างาน list of ClientProgressView. Read-only ClientWpDetailView — one WP's client-visible detail (status + approved photos). (Extends spec 233/234 U4; loadClientWpDetail RLS-scoped, null → redirect /client/[projectId].)                                                                                                                                                                      | bespoke: ← chip → `/client/[projectId]` (backHref)      |
| `/client/access-ended`          | public — no gate (deliberately ungated)                                               | Reached: redirect target from /client when the client has 0 live projects (expired/revoked). Calm terminal notice 'การเข้าถึงสิ้นสุดแล้ว' telling the lapsed client to contact the project director. (Spec 233 / ADR 0067; ungated on purpose so a lapsed client (still role 'client') can reach it after /client forwards here.)                                                                                                | none (terminal notice)                                  |
| `/client/claim`                 | session-only (getClaims → redirect /login); NOT requireRole                           | Reached: PM/director-issued single-use 14-day LINE claim link (buildClientClaimUrl), ?token=…. Token present → confirm card with ClientClaimButton → claim_client_invite → /client; no token → 'ลิงก์ไม่ถูกต้อง' notice. (Spec 233/234; visitor-reachable (fresh LINE signup is 'visitor'); an existing client WITHOUT a token → redirect /client; with token = re-entrant add-project claim.)                                   | none                                                    |
| `/portal`                       | requireRole(["contractor"]) — contractor tier only                                    | Reached: roleHome('contractor') post-login landing. Contractor self-service: doc-completeness status, ข้อมูลของฉัน contact self-edit, emergency + PDPA/background consent, own document uploads, bank-change request, ทีมช่าง crew, payment history. (Spec 130 U3 / ADR 0051 / spec 266 U7; ช่าง now get their own /technician portal, not this page; RLS session client, never admin.)                                          | bespoke: sticky header, logout only (no back chip)      |
| `/portal/claim`                 | session-only (getClaims → redirect /login); NOT requireRole                           | Reached: PM-issued 14-day claim link from contractor-invite-block AND worker-invite-block (buildClaimUrl), ?token=…. Token present → confirm card with ClaimButton → bind account to contractor → /portal; no token → 'ลิงก์ไม่ถูกต้อง' notice. (Spec 130 U3 / ADR 0051; visitor-reachable; an already-bound contractor → redirect /portal.)                                                                                     | none                                                    |
| `/register/office`              | session-only (getClaims → redirect /login in StaffRegisterWorkspace); NOT requireRole | Reached: office-role QR target (the office self-onboard door); no project attribution. Renders StaffRegisterWorkspace variant='office' — role-neutral staff registration form (identity + document uploads + PDPA consent); shows a pending/rejected status card once a registration exists. (Spec 286 U1; office roles not project-scoped so no ?site/?project/?by; approved / technician user → redirect roleHome.)            | none (PageShell form only)                              |
| `/register/technician`          | session-only (getClaims → redirect /login in StaffRegisterWorkspace); NOT requireRole | Reached: on-site technician QR target; SA per-project QR carries ?site (display) / ?project / ?by attribution. Renders StaffRegisterWorkspace variant='field' with a site-attribution banner + the same staff registration form; pending/rejected status card thereafter. (Spec 263 U2 / 264 G1+G2 / 286 U1; approved / technician user → redirect roleHome; shared workspace, variant drives only heading + login-return path.) | none (PageShell form only)                              |
| `/technician`                   | requireRole(["technician"]) — technician only                                         | Reached: roleHome('technician') post-login landing for an approved ช่าง. e-employee card + approval status, 'งานที่ได้รับมอบหมาย' coming-soon placeholder, and the worker portal (WorkerPortalSections: wage history, profile, bank, consents, receipts). (Spec 264 G3 / ADR 0072 §8 / 266 U7 (C); RLS session client self-scoped via workers.user_id; ช่าง portal moved here off /portal.)                                      | bespoke: sticky header, logout only (no back chip)      |

## Nova

| Route               | Gate                                            | Rows / actions →                                                                                                                                                                                                                                                                                                                                                                                                                                        | Back →                       |
| ------------------- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- |
| `/nova`             | requireRole(["super_admin"]) — super_admin only | Reached: settings hub → การเงิน section 'Nova' card (super_admin only). Coin economy snapshot (outstanding/issued/returned/holders), entry cards → /nova/dials, /nova/settlement, /nova/shop, NovaAwardForm (manual award), per-worker balances (link → /nova/worker/[id]), and the append-only ledger. (Spec 162 U1 / ADR 0060–0061; balances DERIVED from coin_postings; coins are points ('เหรียญ'), never baht; RLS server client, super sees all.) | chip → `/settings` (ตั้งค่า) |
| `/nova/dials`       | requireRole(["super_admin"]) — super_admin only | Reached: card 'ค่าปรับ Nova (dials)' on /nova; also linked from /workers. NovaDialsForm — calibrate the economic dials (multiplier, HT cut, level weights) + per-level sell rates (sell_rate_table) via SECURITY DEFINER setters. (Spec 161 U7; nova_dials + sell_rate_table via admin client (zero-grant economics); go-live calibration surface.)                                                                                                     | chip → `/nova` (Nova)        |
| `/nova/settlement`  | requireRole(["super_admin"]) — super_admin only | Reached: card 'สรุป & แบ่งเหรียญ' on /nova. NovaSettlementList — per-project close-out: settle_project (set coin pool) then distribute_project_coins to the crew, once per project. (Spec 161 U8; project_settlements + project_coin_distributions via admin client (zero-grant); acted on via SECURITY DEFINER RPCs.)                                                                                                                                  | chip → `/nova` (Nova)        |
| `/nova/shop`        | requireRole(["super_admin"]) — super_admin only | Reached: card 'ร้าน Nova' on /nova. NovaShopAdmin — manage the shop catalog (the coin sink): price items in coins, activate/deactivate via upsert_shop_item / set_shop_item_active. (Spec 161 U9; shop_items via admin client so inactive items also show; the table itself is authenticated-readable.)                                                                                                                                                 | chip → `/nova` (Nova)        |
| `/nova/worker/[id]` | requireRole(["super_admin"]) — super_admin only | Reached: per-worker balance rows (ยอดเหรียญ list) on /nova. One worker's balance breakdown (total / vested / spendable / unvested), NovaWorkerActions (saver bonus / redeem / confiscate), plus confiscations, redemptions, and ledger. (Spec 161 U12; table via admin client, gated vesting derives (coin\_\*\_balance RPCs) via the RLS server client's super JWT; missing worker → notFound().)                                                      | chip → `/nova` (Nova)        |

## Known seams (recorded, not defects)

- `/settings/wp-grouping-import` has **no nav card yet** — reachable by URL only
  (the settings-hub card arrives with the spec 270 U3 roster work).
- `/catalog/boq-templates` (+ its detail) is **URL-only**: the `/catalog` entry
  link was deliberately retired (spec 239 U2, hide-not-drop), so the BOQ-template
  surfaces have no live nav entry.
- `/accounting/journal/export` has **no in-app link** — direct-URL CSV only,
  deliberately mirroring the payroll export pattern (the /payroll and
  /requests/reports exports both have download links).
- `/payroll` has **no hub-strip or bottom-tab entry of its own** — it is reached
  only via the `/settings` ค่าแรง card (the ตั้งค่า tab lights on it). Same
  placement posture as its money-page siblings.
- `/coming-soon`'s served-role bounces are **stale vs roleHome**: it sends
  site_admin → `/projects` (roleHome says `/sa`, spec 192) and pm/director →
  `/review` (roleHome says `/dashboard`, spec 183). Harmless — both targets
  still admit those roles and served roles rarely land here — but the two
  dispatch maps have drifted; a future one-liner should delegate the bounce to
  `roleHome()` (found by this 2026-07-11 re-audit).
